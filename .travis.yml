sudo: false
language: node_js
node_js:
  - '8.10.0'
script:
  - yarn install
  - yarn test --coverage
  - yarn build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: build
  on:
    branch: master
